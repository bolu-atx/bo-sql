# Catalog module meson.build

catalog_sources = files('catalog.h', 'catalog.cpp')

catalog_lib = static_library('catalog',
    sources: catalog_sources,
    include_directories: inc,
    dependencies: []
)